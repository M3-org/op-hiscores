# ethereum-optimism/infra Daily Update (Jul 17, 2026)
## OVERVIEW 
The day's development focused on a critical bug fix in the `proxyd` component, addressing an RPC error during transaction validation.

## KEY TECHNICAL DEVELOPMENTS

### `proxyd` Transaction Validation Fix
A bug causing an RPC error in `tx_validation_middleware` when failing to close against a failing screening-service/provider has been resolved. This improves the robustness of transaction processing within `proxyd`. ([#669](https://github.com/ethereum-optimism/infra/pull/669))

## NEWLY OPENED PULL REQUESTS
No new pull requests were opened today.

## CLOSED ISSUES
No issues were closed today.

## NEW ISSUES
No new issues were reported today.

## ACTIVE ISSUES
### Kona-node `superroot_atTimestamp` Implementation Blocked
The implementation of the `superroot_atTimestamp` RPC API in `kona-node` ([#21861](https://github.com/ethereum-optimism/infra/issues/21861)) is currently blocked. The latest comment indicates that this feature cannot proceed until `kona-node` gains support for a safe database.