# ethereum-optimism/optimism Daily Update (Jun 6, 2026)
## OVERVIEW 
The day's development focused on updating the `op-devstack` to sequence on `op-reth` for Karst+ in acceptance tests, reflecting the deprecation of `op-geth` for this fork. This involved modifications across several `sysgo` files to ensure compatibility and proper testing.

## KEY TECHNICAL DEVELOPMENTS

### Karst+ Compatibility in Acceptance Tests
The `op-devstack` acceptance tests were updated to sequence on `op-reth` for Karst+ blocks, addressing the deprecation of `op-geth` for this fork. This ensures that the testing harness remains functional and accurate with the latest protocol changes. ([#21188](https://github.com/ethereum-optimism/optimism/pull/21188))

## NEWLY OPENED PULL REQUESTS
No new pull requests were opened today.

## CLOSED ISSUES

### Karst Fork Compatibility
An issue related to switching to `op-reth`-only sequencing for Karst+ in `op-acceptance-tests` was closed. This issue highlighted the deprecation of `op-geth` as of the Karst fork, which refuses to build, seal, or import Karst blocks. ([#21182](https://github.com/ethereum-optimism/optimism/issues/21182))

## NEW ISSUES
No new issues were reported today.

## ACTIVE ISSUES
No active issues with more than 3 comments.