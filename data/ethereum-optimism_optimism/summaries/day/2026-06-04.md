# ethereum-optimism/optimism Daily Update (Jun 4, 2026)

## OVERVIEW 
Today's development focused on significant enhancements to op-reth with new trie-state snapshotting and interop failsafe metrics, alongside critical bug fixes across various components and extensive CI/testing improvements.

## KEY TECHNICAL DEVELOPMENTS

### op-reth Enhancements and Performance
- Implemented a one-time trie-state snapshot feature to improve read performance by allowing current-state lookups at an anchor block, reducing history-bitmap walks ([#20797](https://github.com/ethereum-optimism/optimism/pull/20797)).
- Integrated the new trie-state snapshot into the backfill process, enabling reads from the snapshot during backfill and atomically advancing the snapshot anchor with each prepended block ([#20828](https://github.com/ethereum-optimism/optimism/pull/20828)).
- Added a gauge metric to op-reth's interop transaction pool to monitor the failsafe enabled state, refreshing on every failsafe poll ([#21151](https://github.com/ethereum-optimism/optimism/pull/21151)).
- Introduced a dedicated JSON-RPC error code (`-320602`) for interop filter failsafe rejections, allowing clients to detect failsafe by code instead of string matching ([#21205](https://github.com/ethereum-optimism/optimism/pull/21205)).
- Adjusted miner fee computation in the payload builder to use the pool transaction's `effective_tip_per_gas` before conversion to a consensus transaction ([#20382](https://github.com/ethereum-optimism/optimism/pull/20382)).

### Critical Bug Fixes and Stability Improvements
- Updated the withdrawal proving flow to support super-root dispute games by using L2 timestamps to locate matching L2 headers and selecting the latest usable game ([#21143](https://github.com/ethereum-optimism/optimism/pull/21143)).
- Restored the `op-challenger` runtime image to Ubuntu from Wolfi to resolve issues with `cannon` binary execution due to dynamic linking dependencies ([#21236](https://github.com/ethereum-optimism/optimism/pull/21236)).
- Corrected the `rollup-boost` Docker image to ship the actual binary instead of a cargo-chef stub, ensuring proper functionality with all dependencies ([#21233](https://github.com/ethereum-optimism/optimism/pull/21233)).
- Fixed an issue where the `fork_lock.toml` validation failed due to the `interop` fork being renamed to `lagoon` but the lock entry remaining keyed as `[interop]` ([#21224](https://github.com/ethereum-optimism/optimism/pull/21224)).

### CI/CD and Testing Enhancements
- Refactored CI to use shared Go cache commands for acceptance tests, resolving an issue where the Go module cache was incorrectly saved/restored ([#21213](https://github.com/ethereum-optimism/optimism/pull/21213)).
- Enabled `op-supernode` rewind tests in CI and fixed a bug where `TestRewindToTimestamp` failed on pre-Holocene chains due to `extraData` requirements ([#21206](https://github.com/ethereum-optimism/optimism/pull/21206)).
- Modified CI to test all Go packages by default via an exclude list, addressing a gap where new packages were silently skipped from testing ([#21210](https://github.com/ethereum-optimism/optimism/pull/21210)).
- Moved fault-proof action tests from `op-e2e/actions/proofs` to `rust/kona/tests/proofs` and made them Kona-only, streamlining the testing process ([#21193](https://github.com/ethereum-optimism/optimism/pull/21193)).
- Improved `op-e2e-fuzz` RPC/engine calls by bounding them and streaming output to diagnose intermittent no-output timeouts in CI ([#21203](https://github.com/ethereum-optimism/optimism/pull/21203)).

## NEWLY OPENED PULL REQUESTS

- **Rust Tooling Updates**: Several PRs focus on Rust dependencies and tooling, including pointing to a reth commit with a fix ([#21247](https://github.com/ethereum-optimism/optimism/pull/21247)), updating reth deps to a specific fix branch ([#21235](https://github.com/ethereum-optimism/optimism/pull/21235)), and fixing Rust dependency caching in CI ([#21232](https://github.com/ethereum-optimism/optimism/pull/21232)).
- **op-reth Features**: New features for `op-reth` include one-shot OP block execution for the test engine ([#21242](https://github.com/ethereum-optimism/optimism/pull/21242)), a knob to reproduce state-trie-overlay deadlock ([#21240](https://github.com/ethereum-optimism/optimism/pull/21240)), interop filter decision metrics ([#21234](https://github.com/ethereum-optimism/optimism/pull/21234)), and exposing standard `Pool` public methods ([#21222](https://github.com/ethereum-optimism/optimism/pull/21222)).
- **CI/CD Improvements**: PRs address CI configuration, such as adding a CI config review guide ([#21246](https://github.com/ethereum-optimism/optimism/pull/21246)), de-duplicating command/job definitions ([#21245](https://github.com/ethereum-optimism/optimism/pull/21245)), correcting a typo in `BASE_REVISION` ([#21244](https://github.com/ethereum-optimism/optimism/pull/21244)), and speeding up Rust CI jobs with sccache ([#21227](https://github.com/ethereum-optimism/optimism/pull/21227)).
- **Testing and Devstack**: Updates include lazily generating genesis allocs in `op-e2e` ([#21238](https://github.com/ethereum-optimism/optimism/pull/21238)), un-skipping light-sequencer interop tests in devstack ([#21220](https://github.com/ethereum-optimism/optimism/pull/21220)), and foundational work for an ephemeral chain and read-only queries in `op-reth-test-engine` ([#21216](https://github.com/ethereum-optimism/optimism/pull/21216)).
- **op-conductor**: A new feature for flag-gated interop reorg recovery has been introduced ([#21230](https://github.com/ethereum-optimism/optimism/pull/21230)).

## CLOSED ISSUES

### Metrics and Performance Improvements
- The issue of high cardinality for exported metrics in Kona has been resolved, which was making metrics fetching painful ([#19927](https://github.com/ethereum-optimism/optimism/issues/19927)).
- The `alloy-op-evm` issue regarding dead-on-read `sender` and `evm_gas_used` fields on `OpTxResult` has been closed, as these fields are no longer read after execution ([#20463](https://github.com/ethereum-optimism/optimism/issues/20463)).

### op-reth Proofs and Snapshotting
- The infrastructure for a one-time trie-state snapshot, including schema, lifecycle, and initialization job, has been built and the issue closed ([#20791](https://github.com/ethereum-optimism/optimism/issues/20791)).
- The integration of the trie-state snapshot into the backfill process to eliminate per-key source lookup costs has been completed and the issue closed ([#20792](https://github.com/ethereum-optimism/optimism/issues/20792)).

### Withdrawal and Interop Failsafe Fixes
- The problem with the withdrawal prove script not supporting super roots has been addressed and the issue closed ([#21113](https://github.com/ethereum-optimism/optimism/issues/21113)).
- The issue where the op-reth interop tx pool did not resume after failsafe was disabled without a restart has been resolved ([#21149](https://github.com/ethereum-optimism/optimism/issues/21149)).

### Testing and Documentation
- The issue regarding the `op-up` `TestRun` not being run in CI and requiring the `op-reth` binary has been closed as `op-up` was deleted ([#21202](https://github.com/ethereum-optimism/optimism/issues/21202)).
- The `op-e2e/actions/supernode` `TestRewindToTimestamp` failure due to `extraData` requirements before Holocene has been fixed and the issue closed ([#21201](https://github.com/ethereum-optimism/optimism/issues/21201)).
- The issue of the follow-mode sequencer's `latestHead` failing to re-anchor past a `FollowSource` `forceReset` under EL-sync has been closed, as the test could no longer be reproduced after related fixes ([#21125](https://github.com/ethereum-optimism/optimism/issues/21125)).
- The issue of integrating `op-supernode` with `op-sync-tester` and adding extended nightly sync tests has been partially addressed and closed, with a decision to skip further integration due to limitations of the sync tester ([#21096](https://github.com/ethereum-optimism/optimism/issues/21096)).
- The flaky test `TestL2OutputSubmitter/honest`