Inphi: Focused on enhancing the `optimism` repository, merging two significant pull requests: #22588, which avoids CPU prover setup in network mode, and #22575, which adds ZK challenge support to the `op-challenger` move command, demonstrating a primary focus on other work and tests.