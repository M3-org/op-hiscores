jelias2: Focused on significant feature development, merging a large PR in `ethereum-optimism/optimism#21395` that introduced interop bridge/failsafe test tools, and also opened a new feature PR for `op-supernode` (`ethereum-optimism/optimism#21394`). They also addressed a critical bug by creating and closing `ethereum-optimism/optimism#20934` related to a `SIGBUS` crash in `op-reth`, demonstrating a focus on both new functionality and system stability.