ajsutton: Focused on critical bug fixes, opening two PRs in ethereum-optimism/optimism, specifically addressing witness fetches in "fix(kona-host): retry witness fetches via the get_preimage lo..." (#21269) and VM process teardown in "fix(op-challenger): tear down VM process group on timeout" (#21268), while also providing one approval review.