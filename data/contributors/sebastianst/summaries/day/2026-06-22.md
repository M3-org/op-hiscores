sebastianst: Primarily focused on critical infrastructure updates and documentation, merging a significant PR to wire `op-node` onto `op-core/eip1559` and `txinclude` in `ethereum-optimism/optimism#21508` and addressing a dependency vulnerability by bumping `brotli-decompressor` to 5.0.1 in `ethereum-optimism/optimism#21505`, while also providing substantial review support with 25 reviews.