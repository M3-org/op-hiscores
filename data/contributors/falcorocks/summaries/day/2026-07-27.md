falcorocks: Focused on infrastructure and build process improvements, merging five PRs across `ethereum-optimism/infra` and `ethereum-optimism/optimism` including updates to factory pins and migrating a Dockerfile to a Wolfi base, primarily addressing bugfixes and configuration changes.