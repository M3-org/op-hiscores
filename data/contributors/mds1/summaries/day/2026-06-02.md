mds1: Focused on enhancing the `ethereum-optimism/specs` by merging a significant PR adding a new contract specification (+407/-1 lines in #914), and also addressed an EVM engineering issue in `ethereum-optimism/optimism` by creating and closing #12139.