mds1: Addressed a contract bug by merging PR #21592 in `ethereum-optimism/optimism`, which involved a small but impactful fix to the `OptimismMintableERC20Factory` CREATE2 pre-deployment. This work primarily focused on bugfix work within tests.