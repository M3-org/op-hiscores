pauldowman: Focused on bugfix work, merging a pull request in ethereum-optimism/optimism (#21014) that addressed a CI issue by preserving `mise.toml` for the `kona-client` Docker build, demonstrating attention to build process stability.