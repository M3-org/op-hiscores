hdcesario-op: Focused on infrastructure improvements, merging two PRs that removed the `circleci_ip_ranges` flag and migrated to `circleci-repo-rp` in `ethereum-optimism/superchain-ops` (#1429) and `ethereum-optimism/superchain-registry` (#1233), primarily impacting configuration and tests.