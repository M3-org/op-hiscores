emhane: Focused on enhancing the Optimism ecosystem by creating three issues, including one to add an `op-reth` subsection for running with historical proofs exex in `ethereum-optimism/docs#1894` and two others to re-export proofs exex in `reth-op` and replace `reth-trie-common` in `reth-optimism-trie`.