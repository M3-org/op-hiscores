{
  "version": "1.0",
  "type": "repository",
  "interval": "day",
  "date": "2026-06-04",
  "generatedAt": "2026-06-05T00:28:16.804Z",
  "sourceLastUpdated": "2026-06-05T00:28:16.804Z",
  "contentFormat": "markdown",
  "contentHash": "748b972b9bc4728c35620ae45b2d3664c51698201b97ec8b4bcdd0a950192abb",
  "entity": {
    "repoId": "ethereum-optimism/infra",
    "owner": "ethereum-optimism",
    "repo": "infra"
  },
  "content": "# ethereum-optimism/infra Daily Update (Jun 4, 2026)\n## OVERVIEW \nToday's development focused on enhancing `proxyd` with a new interop validation strategy and fixing a critical bug related to CL backend initialization at network genesis. Several new pull requests were opened to further improve `proxyd`'s functionality and metrics.\n\n## KEY TECHNICAL DEVELOPMENTS\n\n### Enhanced `proxyd` Interoperability and Validation\nA new additive `agreement` interop-validation strategy was added to `proxyd`. This strategy concurrently fans out `checkAccessList` requests to all configured supervisor URLs and combines verdicts by quorum agreement, ensuring robust validation.\n- feat(proxyd): add agreement interop validation strategy ([#640](https://github.com/ethereum-optimism/infra/pull/640))\n\n### `proxyd` Consensus Layer Backend Fix\nA bug in `proxyd`'s CL consensus mode was fixed, allowing CL backends to report block height `0` at network genesis. Previously, `updateCLBackend` rejected any backend reporting block height `0` for `unsafe`, `safe`, `local_safe`, or `finalized`, which was problematic when `op-node` reports block height `0` for all safety levels at genesis.\n- fix(proxyd): allow CL backends at network genesis (block 0 with valid hash) ([#645](https://github.com/ethereum-optimism/infra/pull/645))\n\n## NEWLY OPENED PULL REQUESTS\n- [#644](https://github.com/ethereum-optimism/infra/pull/644) refactor(proxyd): rename interop validation supervisor naming to interop filter\n- [#643](https://github.com/ethereum-optimism/infra/pull/643) feat(proxyd): add metrics for interop validation outcomes\n- [#642](https://github.com/ethereum-optimism/infra/pull/642) feat(proxyd): add metrics for interop validation outcomes\n- [#646](https://github.com/ethereum-optimism/infra/pull/646) feat(proxyd): halt CL consensus on unresolvable output root split\n\n## CLOSED ISSUES\n\n### `op-reth` Interop Transaction Pool Resolution\nThe issue where `op-reth`'s transaction pool failed to resume accepting interop transactions after the interop-filter failsafe was disabled has been resolved. The fix involved restarting both `op-reth` and `op-rbuilder` on affected sequencers. A new metric is proposed to observe the failsafe state.\n- op-reth: interop tx pool does not resume after failsafe is disabled (requires restart) ([#21149](https://github.com/ethereum-optimism/infra/issues/21149))\n\n### `op-supernode` Integration and Sync Testing\nThe integration of `op-supernode` with `op-sync-tester` and the addition of an extended nightly sync test has been partially completed. The initial part of the work has been shipped, with other parts being skipped as longer-running tests should not use sync tester due to its EL mocking.\n- Integrate op-supernode with op-sync-tester + add extended nightly sync test ([#21096](https://github.com/ethereum-optimism/infra/issues/21096))\n\n### Sequencer Follow-Mode Stability\nA follow-up issue regarding the follow-mode sequencer's `latestHead` failing to re-anchor past a `FollowSource` `forceReset` under EL-sync has been closed. After extensive testing, the flake could not be reproduced, suggesting the issue was related to how a previous invalid message replacement test was written.\n- Follow-mode sequencer's `latestHead` fails to re-anchor past a `FollowSource` `forceReset` under EL-sync (follow-up to #21119) ([#21125](https://github.com/ethereum-optimism/infra/issues/21125))\n\n### `op-up` Test Removal\nThe `op-up` test (`TestRun`) that was not running in CI due to its dependency on the `op-reth` binary has been addressed by the deletion of the `op-up` package itself.\n- op-up: TestRun not run in CI; requires op-reth binary the go-tests job doesn't build ([#21202](https://github.com/ethereum-optimism/infra/issues/21202))\n\n## NEW ISSUES\nNo new issues were opened today.\n\n## ACTIVE ISSUES\n\n### `op-e2e-fuzz` Flakiness Investigation\nThe `op-e2e-fuzz` CircleCI job intermittently fails with \"Too long with no output (15m)\". The root cause has been identified as `op-e2e/config`'s package `init()` running a full genesis pipeline for all alloc types and hardfork modes at binary startup, leading to minutes of CPU usage before any fuzz body execution. This oversubscribes the 8-vCPU box when 3 concurrent `go test -fuzz` processes run. The fix involves generating each alloc type lazily and once. A draft PR ([#21238](https://github.com/ethereum-optimism/optimism/pull/21238)) is in progress.\n- [Flake] op-e2e-fuzz: 'Too long with no output (15m)' — parallel buffers fuzz output until job completion ([#21191](https://github.com/ethereum-optimism/infra/issues/21191))\n\n### `op-reth-test-engine` for `op-e2e/actions`\nImplementation is underway for `op-reth-test-engine` to provide a minimal in-test EL for `op-e2e/actions`. The approach reuses the real `op-reth` stack, including `OpChainSpec`, `OpNode` provider factory, `init_genesis`, and `BlockchainProvider`. The first PR in a stacked series, focusing on the `EphemeralChain` foundation, is open ([#21216](https://github.com/ethereum-optimism/optimism/pull/21216)).\n- op-reth-test-engine: minimal in-test EL for op-e2e/actions (and Rust action-test parity) ([#20415](https://github.com/ethereum-optimism/infra/issues/20415))\n\n### `kona`: High Cardinality for Exported Metrics\nThe issue of very high cardinality for exported metrics in `kona` has been fixed in a related PR ([#21223](https://github.com/ethereum-optimism/optimism/pull/21223)).\n- kona: Very high cardinality for exported metrics ([#19927](https://github.com/ethereum-optimism/infra/issues/19927))\n\n### Flaky Test: `TestL2OutputSubmitter/honest`\nA flaky test `TestL2OutputSubmitter/honest` was reported, but upon investigation, it was determined to be filed in error. The PR it was associated with was Rust-only, so the Go `op-e2e` suite was skipped, and the test was never actually run in that context.\n- flaky test: `TestL2OutputSubmitter/honest` — context deadline exceeded (op-e2e action/proofs) ([#21231](https://github.com/ethereum-optimism/infra/issues/21231))"
}